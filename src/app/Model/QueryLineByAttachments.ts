import {QueryLine} from './QueryLine';

export class QueryLineByAttachments {
  id: number;
  queryLine: QueryLine;
  attachmentUrl: string;
  attachmentName: string;
  attachmentContentType: string;

}
